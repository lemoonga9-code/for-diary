# 电子手帐 项目指引

## 项目简介

一个运行在 Mac 浏览器里的单文件电子手帐工具：记录每日日记/待办/做了什么、上传照片、一键生成拼贴排版手帐、按风格切换素材。用户是完全不懂代码的小白，交流使用简体中文，重要步骤需先与用户确认。

## 标准文件路径

- 产品文件：`电子手帐.html`（项目根目录，**唯一的产品代码文件**，双击用浏览器打开）
- `素材/文字/`：用户提供的原始文字（需求描述、参考文案等）
- `素材/图片/`：用户提供的原始图片（参考布局、示例照片等）
- `素材/风格素材/`：用户提供的风格参考素材（每个新风格在此放 1-2 个参考图）
- 开发计划：`/Users/lemoonga-/.claude/plans/precious-wiggling-hejlsberg.md`（阶段清单与验收标准）

## 硬性约束（不可违反）

1. 所有功能代码只写在 `电子手帐.html` 一个文件内：CSS 在 `<style>`，JS 在 `<script>`，禁止外部库、CDN、构建工具、npm 依赖。
2. UI 全部使用简体中文。
3. 整体配色低饱和度（奶油、雾蓝、灰粉等温柔色）。
4. 数据只保存在浏览器本地（IndexedDB/localStorage），不上传网络；只有用户已填 API key 时，AI 排版才会把当天日记文字发给 AI 接口。
5. 断网时除 AI 排版外全部功能必须正常（AI 排版失败自动降级为本地生成）。

## 素材工作流

1. 用户把风格参考素材放进 `素材/风格素材/`。
2. 查看参考图，提取风格特征（配色、造型、花纹）。
3. 用 AI/手工生成一批同风格 SVG 矢量贴纸（原创通用图形，不复制受版权保护的角色或图案）。
4. 生成预览给用户确认，用户不满意可提出调整。
5. 确认后把 SVG 内置进 `电子手帐.html` 的素材清单（`STYLE.stickers`，每张贴纸含 `id`、心情/主题 `tag`、`svg`）。当前已内置 62 枚「粉彩可爱风」贴纸（6 个原创角色 × 大头/全身 + 装饰小物 + 画框 + 文字贴），心情识别规则在 `MOODS`。

## 工作方式

- 按开发计划分阶段推进；**每完成一个阶段停下来等用户试用确认**，确认后才进入下一阶段。
- 每个新风格 = 重复"素材工作流 + 生成引擎适配"。
- 改动前先读本文件。
- 数据注意事项：html 文件改名或移动位置可能导致浏览器找不到旧数据 → 引导用户固定文件位置、常备份。

## 数据模型（约定）

- IndexedDB 库 `shouzhang`：
  - `entries`：`{ id, date, diary, didToday[], todoSnapshot[], images[{id, blob, note}], mood, style, layout, updatedAt }`
  - `todos`：`{ id, text, done, createdAt, doneAt }`（持续清单，未完成自动顺延到第二天）
- 设置（API key、模型、接口地址、默认风格）存 localStorage。
